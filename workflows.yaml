name : my_first_workflow

on : 
  push :
    branches : main

jobs : 
  first_job : 
    runs-on : ubuntu-latest

    steps : 

      - name: Checkout
        uses: actions/checkout@v6.0.2
      
      - name : run_script
        run : ls -ltra 

      - name : run_script
        run : |
         chmod +x script.sh
         ./script.sh


      
